name: Telegram Media to Google Drive Sync

on:
  schedule:
    - cron: '0 */6 * * *'
  workflow_dispatch:

jobs:
  run-media-sync:
    runs-on: ubuntu-latest
    permissions:
      contents: write

    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: '3.11'

      - name: Install dependencies
        run: |
          python -m pip install --upgrade pip
          pip install telethon google-api-python-client google-auth-httplib2 google-auth-oauthlib python-dotenv

      - name: Run Media Sync Script
        env:
          TELEGRAM_API_ID: ${{ secrets.TELEGRAM_API_ID }}
          TELEGRAM_API_HASH: ${{ secrets.TELEGRAM_API_HASH }}
          TELEGRAM_SESSION: ${{ secrets.TELEGRAM_SESSION }}
          GDRIVE_FOLDER_ID: ${{ secrets.GDRIVE_FOLDER_ID }}
          GOOGLE_TOKEN_JSON: ${{ secrets.TOKEN_JSON }} # ⚠️ تأكد أن هذا الاسم مطابق تماماً للاسم في الإعدادات
          MAX_UPLOADS: 1000
        run: |
          python media_to_gdrive.py

      - name: Commit and Push state changes
        run: |
          git config --global user.name "github-actions[bot]"
          git config --global user.email "github-actions[bot]@users.noreply.github.com"
          
          git add media_state.json
          
          git diff --quiet && git diff --staged --quiet || git commit -m "Auto-update media state [skip ci]"
          git push
          
